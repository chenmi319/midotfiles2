# =============================================================================
# macOS                https://github.com/github/gitignore/blob/main/Global/macOS.gitignore
# =============================================================================
.DS_Store
# Thumbnails
._*
# Resource forks on external volumes
.__MACOSX/
.AppleDouble
.LSOverride
# Icon\r\r — macOS custom icon file; exact pattern requires literal CR bytes
# which most editors will strip, so we use bare "Icon" (may overmatch, but
# files named exactly "Icon" without CR suffix are virtually nonexistent)
Icon


# Files that might appear in the root of a volume
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# Directories potentially created on remote AFP share
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# =============================================================================
# Windows             https://github.com/github/gitignore/blob/main/Global/Windows.gitignore
# =============================================================================
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# =============================================================================
# Vim/Neovim          https://github.com/github/gitignore/blob/main/Global/Vim.gitignore
# =============================================================================
# Swap
[._]*.s[a-v][a-z]
!*.svg
[._]*.sw[a-p]
[._]s[a-rt-v][a-z]
[._]ss[a-gi-z]
[._]sw[a-p]

# Session
Session.vim
Sessionx.vim

# Temporary
.netrwhist
*~


# Persistent undo
[._]*.un~

# =============================================================================
# Tags                https://github.com/github/gitignore/blob/main/Global/Tags.gitignore
# =============================================================================
TAGS
.TAGS
!TAGS/
tags
!tags/
.tags
GTAGS
GRTAGS
GPATH
GSYMS
gtags.files
cscope.files
cscope.out

# =============================================================================
# AI & coding tools
# =============================================================================
# Gemini
.gemini/
GEMINI.md

# Claude / Anthropic
CLAUDE.md
.claude

# GitHub Copilot
.github/copilot-instructions.md

# Other AI coding assistants
QWEN.md
CODEBUDDY.md
.qoder/
.taskmaster/

# Serena
.serena/

# OpenCode
opencode.jsonc
opencodetmp/
.tmp

# Beans (issue tracker)
.beans.yml
.beans/

# Planning files
.planning/

# =============================================================================
# Security safeguard
# =============================================================================
# Environment files (only definite private files; templates belong in project .gitignore)
.env
.env.local
.env.*.local

# SSH private keys & credentials (universally private; other secrets belong in project .gitignore)
id_rsa
id_ed25519
.netrc
