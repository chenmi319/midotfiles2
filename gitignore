# =============================================================================
# macOS                https://github.com/github/gitignore/blob/main/Global/macOS.gitignore
# =============================================================================
.DS_Store
# 缩略图
._*
# 外部卷上的资源分支
.__MACOSX/
.AppleDouble
.LSOverride
# Icon\r\r — macOS 自定义图标文件；精确匹配需要字面 CR 字节，
# 但大多数编辑器会将其去除，所以这里用 "Icon"（可能误匹配，
# 但实际上几乎不存在恰好名为 "Icon" 且不带 CR 后缀的文件）
Icon


# 可能出现在卷根目录的文件
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent

# 远程 AFP 共享上可能创建的目录
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# =============================================================================
# Windows             https://github.com/github/gitignore/blob/main/Global/Windows.gitignore
# =============================================================================
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
[Dd]esktop.ini
$RECYCLE.BIN/
*.cab
*.msi
*.msix
*.msm
*.msp
*.lnk

# =============================================================================
# Vim/Neovim          https://github.com/github/gitignore/blob/main/Global/Vim.gitignore
# =============================================================================
# 交换文件
[._]*.s[a-v][a-z]
!*.svg
[._]*.sw[a-p]
[._]s[a-rt-v][a-z]
[._]ss[a-gi-z]
[._]sw[a-p]

# 会话文件
Session.vim
Sessionx.vim

# 临时文件
.netrwhist
*~


# 持久撤销
[._]*.un~

# =============================================================================
# Tags                https://github.com/github/gitignore/blob/main/Global/Tags.gitignore
# =============================================================================
TAGS
.TAGS
!TAGS/
tags
!tags/
.tags
GTAGS
GRTAGS
GPATH
GSYMS
gtags.files
cscope.files
cscope.out

# =============================================================================
# AI 及编码工具
# =============================================================================
# Gemini
.gemini/
GEMINI.md

# Claude / Anthropic
CLAUDE.md
.claude

# GitHub Copilot
.github/copilot-instructions.md

# 其他 AI 编码助手
QWEN.md
CODEBUDDY.md
.qoder/
.taskmaster/

# Serena
.serena/

# OpenCode
opencode.jsonc
opencodetmp/
.tmp

# Beans（议题追踪器）
.beans.yml
.beans/

# 规划文件
.planning/

# =============================================================================
# 安全防护
# =============================================================================
# 环境变量文件（仅限明确的私密文件；模板文件应在项目级 .gitignore 中处理）
.env
.env.local
.env.*.local

# SSH 私钥与凭据（通用私密文件；其他密钥应在项目级 .gitignore 中处理）
id_rsa
id_ed25519
.netrc

# 规划文件（planning-with-files）
task_plan.md
findings.md
progress.md
